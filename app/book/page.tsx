"use client"
import { useState } from "react"
import Link from "next/link"
const services=["The Signature Cut — €48","Cut & Beard — €62","The Classic Shave — €38","Father & Son — €70"]
export default function Book(){const [step,setStep]=useState(1); const [done,setDone]=useState(false); return <main className="min-h-screen px-5 py-10 md:px-10"><Link href="/" className="text-xs tracking-[.2em]">← CUT / THRU</Link><div className="mx-auto max-w-2xl py-20"><p className="text-xs uppercase tracking-[.2em] text-copper">Appointment booking · Step {step} of 7</p><div className="mt-5 h-1 bg-line"><div className="h-full bg-copper transition-all" style={{width:`${step/7*100}%`}}/></div>{done?<section className="py-24 text-center"><h1 className="display text-6xl">See you in the chair.</h1><p className="mt-6 text-muted">Your booking reference is <strong>CT-2408</strong>.</p></section>:<form onSubmit={e=>{e.preventDefault();step===7?setDone(true):setStep(step+1)}} className="mt-20">{step===1&&<><h1 className="display text-5xl">Choose your service.</h1><div className="mt-10 grid gap-3">{services.map(s=><label key={s} className="border line p-5 has-[:checked]:border-copper"><input required type="radio" name="service" className="mr-3 accent-copper"/> {s}</label>)}</div></>}{step===2&&<><h1 className="display text-5xl">Choose your barber.</h1><select required className="mt-10 w-full border line bg-ink p-4"><option>Any available barber</option><option>Milo van Dijk</option><option>Jules Bakker</option><option>Amara de Wit</option></select></>}{step===3&&<><h1 className="display text-5xl">Choose a date.</h1><input required type="date" className="mt-10 w-full border line bg-ink p-4"/></>}{step===4&&<><h1 className="display text-5xl">Choose a time.</h1><div className="mt-10 grid grid-cols-3 gap-3">{["09:00","10:30","12:00","14:00","15:30","17:00"].map(t=><label key={t} className="border line p-4 text-center has-[:checked]:border-copper"><input required type="radio" name="time" className="sr-only"/>{t}</label>)}</div></>}{step===5&&<><h1 className="display text-5xl">About you.</h1><div className="mt-10 grid gap-4">{["Name","Email","Phone"].map((l,i)=><label key={l} className="text-xs uppercase tracking-widest">{l}<input required type={i===1?"email":"text"} className="mt-2 w-full border line bg-transparent p-4"/></label>)}<label className="text-xs uppercase tracking-widest">Notes <textarea className="mt-2 w-full border line bg-transparent p-4"/></label></div></>}{step===6&&<><h1 className="display text-5xl">Review your booking.</h1><div className="mt-10 border line p-6 leading-8 text-muted">The Signature Cut<br/>Any available barber<br/>Your selected date and time<br/><strong className="text-paper">Total · €48</strong></div></>}{step===7&&<><h1 className="display text-5xl">Payment.</h1><p className="mt-6 text-muted">Payment will be collected at the shop. This placeholder reserves your appointment.</p></>}<div className="mt-12 flex justify-between">{step>1&&<button type="button" onClick={()=>setStep(step-1)} className="border line px-5 py-3 text-xs uppercase tracking-widest">Back</button>}<button className="ml-auto bg-copper px-6 py-3 text-xs font-bold uppercase tracking-widest text-ink">{step===7?"Confirm booking":"Continue"}</button></div></form>}</div></main>}
